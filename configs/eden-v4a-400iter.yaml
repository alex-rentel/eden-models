# Eden v4a: Extended training — 400 iters (v4 val loss still dropping at 200)
model: mlx-community/Qwen3-1.7B-4bit
train: true
data: data/
num_layers: 8
batch_size: 4
iters: 400
learning_rate: 2e-6
steps_per_report: 10
steps_per_eval: 100
save_every: 100
max_seq_length: 2048
mask_prompt: true
adapter_path: adapters/qwen3-1.7b-tools-v4a
seed: 42
lora_parameters:
  rank: 8
  scale: 10.0
  dropout: 0.0
